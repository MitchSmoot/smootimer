<%= render "shared/main_nav" %>

<h1>Default Timer</h1>

<!-- <script>
    const toggleTimer = (e) => {
        let windowEvent = window.event ? event : e;
        if (windowEvent.keyCode === 32) {
            Timer.toggle
        }
    }
</script> -->


<p><%= Solve.all %></p>

<p><%= Solve.last.solve_time %></p>
<p><%= Solve.last.user %></p>